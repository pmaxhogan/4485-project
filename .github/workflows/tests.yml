name: Run unit tests and e2e tests on push or pull-request

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run unit tests
        run: npm run test:unit
  e2e-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Run e2e requirements tests
        run: | 
          npm run test:e2e:driver &
          sleep 10 &&
          npm run test:e2e
          
      - name: Run e2e bench/NFR tests
        run: | 
          npm run test:e2e:driver &
          sleep 10 &&
          npm run test:e2e:bench
